machine:
  node:
    version: 5.5.0
dependencies:
  post:
    - npm run lint
    - npm run build
test:
  override:
    - npm run test -- --reporters junit
    - mv TESTS*.xml $CIRCLE_TEST_REPORTS/
general:
  artifacts:
    - dist
