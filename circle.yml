machine:
  python:
    version: 3.5.0
database:
  override:
    - mysql -u ubuntu -e "create database yunity_database"
    - mysql -u ubuntu -e "create database yunity_test"
    - cp config/local_settings.py.ci config/local_settings.py
