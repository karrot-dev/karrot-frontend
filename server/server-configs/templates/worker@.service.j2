[Unit]
Description=Django Worker
After=network.target
PartOf={{ name }}-worker.target

[Service]
Type=simple
User={{ user }}
Group={{ group }}
WorkingDirectory=/var/www/{{ name }}/www
ExecStart=/var/www/{{ name }}/www/env/bin/python manage.py runworker
Restart=always