import { AppVisibility, Notify, ClosePopup, Dialog, TouchHold } from 'quasar'

export default {
  plugins: {
    Dialog,
    AppVisibility,
    Notify,
  },
  directives: {
    ClosePopup,
    TouchHold,
  },
}
