/* default configuration values, override by creating a yConfig.local.js file */
export default {
  /*
    Which backend to use to proxy /api requests to
    Defaults to public
  */
  backend: 'https://dev.yunity.org'
};